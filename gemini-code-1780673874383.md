# Novel2Script AI
**基于大语言模型的小说转剧本创作辅助系统技术设计文档**

---

## 1. 项目概述

### 1.1 项目背景
随着网络文学和数字阅读的蓬勃发展，大量优质原创小说具备了极高的影视化改编潜力。然而，传统的“小说转剧本”过程需要编剧耗费大量精力对人物关系、剧情结构、场景设定及对白进行提取与重构，具有显著的高门槛、长周期和高成本痛点。

近年来，大语言模型（LLM）在长文本理解、复杂语义抽取与结构化数据生成方面取得了突破性进展，为影视工业链条前端的“自动化剧本解析”提供了坚实的技术底座。本项目旨在打造一款高效的生产力工具，赋能创作者完成从非结构化文本到结构化剧本的快速跨越。

### 1.2 核心目标
* **实现超长文本处理**：稳定支持 3 个章节以上（数万字级别）小说的无损长上下文上传与解析。
* **高精度实体抽取**：自动化识别并构建结构化的角色注册表（包含姓名、身份、性格与行为动机）与时空设定。
* **智能场景切分**：突破物理章节限制，按戏剧逻辑自动重组并输出标准的“场景（Scene）”流。
* **标准化工程输出**：最终生成严格符合影视工业标准的结构化 YAML 剧本文件，支持后续的跨平台无缝协同与人工二次打磨。

---

## 2. 系统总体架构设计

系统采用现代化的全栈微服务架构设计，坚持前后端解耦与数据流单向响应原则，确保系统的高可用性与可扩展性。

* **前端展现层 (Presentation Layer)**：基于 Next.js 与 React 构建的高性能 SPA 应用。提供沉浸式双栏工作台（小说原文与剧本 YAML 实时映射），并集成小说上传、流式剧本渲染与一键导出模块。
* **业务服务层 (Service Layer)**：基于 Node.js (Express) 构建的核心后端。负责统一网关路由、身份鉴权、文本预处理（Chunking & 清洗），以及全双工流式通信（SSE/WebSocket）管理。
* **AI 调度层 (AI Orchestration Layer)**：核心引擎层。封装底层大模型（如 GPT-4o, DeepSeek-V3）接口，实现基于 LangChain 思想的多 Agent 复杂任务图编排与状态上下文管理。
* **数据持久层 (Data Access Layer)**：采用 PostgreSQL 作为关系型数据库，利用 Prisma ORM 实现强类型安全的数据交互，确保角色图谱与场景 YAML 碎片的数据一致性。

---

## 3. 核心功能模块与大模型 Agent 协同网络

系统摒弃了低效的单轮 Prompt 策略，创新性地采用**多 Agent 协作工作流 (Multi-Agent Collaborative Workflow)**，将复杂的改编任务拆解为四个专业子域：

> **Agent 1: 剧情解构者 (Novel Analyzer)**
> * **职责**：承担全局视角的文本通读与剧情降维任务。
> * **输入流**：预处理后的小说文本分块 (Text Chunks)。
> * **输出流**：结构化故事大纲、核心主题向量、全局时间线 (Timeline)。

> **Agent 2: 角色图谱构建师 (Character Analyzer)**
> * **职责**：在全局范围内进行命名实体识别与性格推理，防止后续章节中出现“角色幻觉”或“称呼不一致”。
> * **输出流**：包含 `[姓名, 别名, 核心身份, 性格标签, 当前阶段目标]` 的全局角色注册表集合。

> **Agent 3: 场记统筹 (Scene Planner)**
> * **职责**：根据剧情摘要和原文流，划定戏剧冲突边界，将流水账文本转化为可拍摄的物理场景。
> * **输出流**：场景元数据集合，包括 `[场景编号, 室内外/地点, 日夜/时间, 当前在场角色 ID 列表]`。

> **Agent 4: 剧本主笔 (Script Writer)**
> * **职责**：接收前三个 Agent 的结构化约束，对具体的场景进行微观的动作与对白生成，确保符合人物性格特征。
> * **输出流**：严格符合预设 Schema 的标准 YAML 剧本节点数据。

---

## 4. 数据库实体建模 (Database Schema)

基于 Prisma ORM 设计的 PostgreSQL 实体关联模型如下：

| 数据表名 | 字段定义说明 | 核心作用与关联关系 |
| :--- | :--- | :--- |
| **Novel (项目原著表)** | `id` (UUID, PK)<br>`title` (String)<br>`content` (Text)<br>`status` (Enum)<br>`created_at` (DateTime) | 存储用户上传的原始文档数据与整体项目状态。 |
| **Character (角色注册表)** | `id` (UUID, PK)<br>`novel_id` (FK)<br>`name` (String)<br>`aliases` (String[])<br>`role_type` (String)<br>`traits` (JSONB) | 强关联至具体小说项目，利用 JSONB 存储灵活的性格标签集。 |
| **Scene (场景轴表)** | `id` (UUID, PK)<br>`novel_id` (FK)<br>`scene_num` (Int)<br>`location` (String)<br>`time_of_day` (String) | 剧本的空间和时间锚点，按 `scene_num` 排序串联整个时间线。 |
| **Script (剧本内容表)** | `id` (UUID, PK)<br>`scene_id` (FK)<br>`yaml_content` (Text)<br>`version` (Int) | 存储大模型最终生成的 YAML 源码，支持版本迭代与回滚机制。 |

---

## 5. 技术栈清单与部署架构

* **前端工程**：Next.js 14, React 18, TypeScript, TailwindCSS, Zustand, Monaco Editor (YAML 编辑)。
* **后端引擎**：Node.js, Express, 响应流 (Server-Sent Events)。
* **数据存储**：PostgreSQL (核心业务), Prisma ORM (类型安全查询)。
* **AI 驱动模型**：GPT-4o (复杂推理/核心生成), DeepSeek-V3 (长文本高性价比吞吐)。
* **部署拓扑**：采用 Docker 容器化打包，前端与后端服务分别部署，由 Nginx 提供反向代理与 HTTPS 卸载。

---

## 6. 核心创新点与技术壁垒

1.  **多 Agent 异步流水线架构**：彻底解决了传统单节点 LLM 在处理超长小说时极易出现的“上下文遗忘”和“逻辑链断裂”问题。
2.  **强校验机制的 YAML 结构化输出**：避免了“自然语言剧本”无法被系统二次解析的难题，为下游的 AI 视频生成（Text-to-Video）或自动化分镜脚本铺平了道路。
3.  **动态数据图谱**：系统不仅输出文本，还能在前端实时动态绘制“角色交互关系网络图”与“剧情起伏时间线”，极大提升了创作者的宏观把控能力。

---

## 7. 预期工程效能提升

部署本系统后，预期可将单部 5 万字中篇小说的初期粗排剧本创作时间从传统的 2-3 周压缩至 30 分钟以内，整体基础改编效率提升 80% 以上，使人类编剧能够将核心精力回归到深度的艺术打磨与二次创作中。